#!/usr/bin/env bash
source "$HOME/.bash_functions/strict.bash"
brew-try-uninstall lua
for name in lua lua-language-server; do
	mise install $name@latest
	mise use --global $name@latest
	mise-uninstall-old $name
done
