#!/usr/bin/env bash -e
default_branch="$(git-default-branch)" || exit $?
cmd="git fetch origin $default_branch"
branch="$(git-branch-name)" || exit $?
if [ "$branch" != "$default_branch" ]; then
	cmd="$cmd $branch"
fi
$cmd
