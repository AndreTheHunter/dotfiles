#!/usr/bin/env bash
#shellcheck source=../strict.bash
source ~/strict.bash
default_branch="$(git-default-branch)"
cmd="git fetch origin $default_branch"
branch="$(git-branch-name)"
if [ "$branch" != "$default_branch" ]; then
	cmd="$cmd $branch"
fi
$cmd
