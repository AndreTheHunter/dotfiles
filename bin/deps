#!/usr/bin/env fish
set -l start $PWD
for file in (bfs -name 'package.json' -exclude -name 'node_modules')
    show cd (path dirname $file)
    if test -e 'yarn.lock'
        show yarn install
    else
        show npm install
    end
    cd $start
end
