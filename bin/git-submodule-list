#!/usr/bin/env sh
toplevel=$(git rev-parse --show-toplevel 2>/dev/null)
[ -z "$toplevel" ] && exit 0
[ -f "$toplevel/.gitmodules" ] || exit 0

git submodule status --recursive "$@" | sed -E 's/^.{42}//; s/ \([^()]*\)$//'
