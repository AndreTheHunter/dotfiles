#!/usr/bin/env fish
clear
set -l cmd (project_delegate)

if test -n "$cmd"
    if test "$cmd" = "godot"
        show godot --editor .
    else
        show $cmd $argv
    end
else
    err "Not a project"
    exit 1
end
