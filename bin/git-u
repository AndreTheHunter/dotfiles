#!/usr/bin/env bash
#shellcheck source-path=..
source "$HOME/.bash_functions/strict.bash"
branch="$(git branch-name)"
if git-remote-branch-exists "$branch"; then
	git-fe "$branch"
	git rebase "origin/$branch"
fi
